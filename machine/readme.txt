Welcome to QuattroOS.
This drive is a real folder
on your host disk: machine/
The OS you are running was read off it at boot: the ROM found bin/os.bin,
streamed it into code memory through the load port, and jumped to it.
Drop files in here and they show up in the list. Delete bin/os.bin and the
machine has nothing to boot -- it will tell you so.
